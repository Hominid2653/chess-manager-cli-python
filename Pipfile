[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
rich = ">=15.0.0"

[dev-packages]
pytest = ">=9.0.0"

[requires]
python_version = "3.14"

[scripts]
chess = "chess"
test = "pytest tests/ -v"
